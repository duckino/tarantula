# Tarantulas
## Overview
Extenable Web Scrapper with plug-and-use framework
Requirements:
* Python 3+

## QuickStart:
- With `pipenv`:
  * `pipenv install`
  * `pipenv shell`
- With `venv`:
  * activate with `pip install -r requirements.txt`
  * `python main.py`